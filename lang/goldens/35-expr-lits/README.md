# 35-expr-lits

Проверяет: решение **68** / **82** / **85** / **89** — `$Ports: !ref` с quoted `?? [80, 443]` (константный list не в `!expr`; кавычки YAML 1.2 из-за `[`); `$Labels` map-литерал в `!expr` с `$` и quoted ключами.
