# Decisions log (locked Q&A)

Формат: решение → смысл → почему (кратко). Не пересматривать без явного запроса.

Строки **8–9, 16, 20, 24–25, 32, 41, 51–53, 59–60, 62–65, 68, 73, 81–132** — дословно из `StrReplace` транскрипта. **1–7, 10–15, 17–19, 21–23, 26–31, 33–40, 42–50, 54–58, 61, 66–67, 69–72, 74–80, 133** — по [SPEC.md](SPEC.md) / [NONGOALS.md](NONGOALS.md) и сохранённому фрагменту Q&A 1–14.

| # | Decision | Meaning | Why |
|---|----------|---------|-----|
| 1 | **A** | Render-only product | Нужен предсказуемый манифест-пайплайн, не ещё один package manager |
| 2 | **A+B → full** | Переписать **существующие** Helm-чарты, включая сложность tpl (if/range/include/sprig/Release/Files), **без** Go `tpl` | Иначе продукт не замена `helm template` |
| 3 | **A** | Multi-doc YAML на выходе | Совместимо с `kubectl`/GitOps без обёрток |
| 4 | **A** + Capital | `$` meta; `$Name` с заглавной | `@` нельзя (YAML reserved); `$` читается как meta; Capital отделяет user bindings от `$when`/`$items` |
| 5 | **A** + 5.1 **B** + **27B** | `!policy` только схема. Нет документа + есть **`!$Type`** → **strict**. Есть `!policy` и **нет typed bind** → ошибка. Не `$Policy:` | Policy не missing `!ref` |
| 6 | **B → typedef** + **28D** | `!typedef` + instance **`$Name: !$Type`**. Default в поле схемы. Нет `$type`, нет `!applyDefaults` | Тег = BindingName; не путать с `!ref $Type` |
| 7 | **A → D + 48 + 49** | Обычный доступ = `!ref` (в т.ч. `['ключ']`). **`!path` снят**. **`!walk` нет навсегда** | Helm `.Values` и `index` — один тег. `from` не `$Name` — `!expr` |
| 8 | **24–25** + **27** + **130** | Missing пути без `?` — **всегда** ошибка (не `!policy`). Нет **значения** `null`. **`a: null` ≡ нет ключа (130)**. Omit ключа stdout — **`ключ?:`** + путь с `?` | Пути явные |
| 9 | **B+H** | `$when` на документе `!emit`; `$else-yield: ""` пропуск; предикат — `!ref` / `!not` / **`!expr`** / YAML **`true`/`false` (85)**. **`$items` нет (38)** | Узкий слой ветвления **одного** документа |
| 10 | **C** + **38** + **39** + **52** + **148** | `!foreach`: `$over`/`$as`/`$yield` или `$yield?:`. **`!emit-foreach`:** `$yield:`; **`!emit-foreach?`:** `$yield?:` (omit → нет документа). Опц. **`$filter`**. `$as` — объявление. `$index` нет | Цикл не эмитит сам. Нет splat `$over` |
| 11 | **A** + trace | Ошибки + `--trace` (формат — **56**, не v1) | Отладка без изменения семантики |
| 12 | **B** | Спека/доки в v1 | Язык без нормативного текста не переносим |
| 13 | local paths | `!read` / `!import` scalar; путь от файла с тегом; `..` и abs OK; нет overlay/URI | Всё в файлах. Не jail: chart может читать любой локальный путь |
| 14 | **B** | Rust | Один бинарник, предсказуемый CLI, без Helm runtime |
| 15 | **`!emit` документ** | Тег на документе, не на значении. `$Name: !emit` — ошибка. Непомеченный манифест — ошибка. Две формы: тело манифеста или `$when`/`$yield`/`$else-yield` (**133**: if без else — `!emit?`) | Один документ stdout = один `!emit` |
| 16 | **`!ref` scalar** | `!ref $X` / `$X.name` / `$X[0].name` / `?.` / **`['ключ']`** (**48**); omit ключа только с **`имя?:`**. Default **`??` — 82 / 83** (один на весь скаляр). Кавычки — **89** | Явный маркер; нет голого `$` и нет `null` |
| 17 | **порядок ключей C** | Порядок mapping — **как во входе**, не алфавит. Sequence / `$over` map — порядок списка или ключей источника. Goldens byte-for-byte | Детерминизм без sort |
| 18 | **сериализация C** | YAML 1.2, UTF-8, LF, 2 пробела, block-style. Первый документ без `---`. Нет `null` в stdout. Пустой поток = 0 байт, exit 0 | Один канон stdout |
| 19 | **eval A** | Forward-ref OK; цикл = ошибка с цепочкой `$Name`; bind атомарный | Декларативно, не «шаблон сверху вниз». Поля одного `$A` не видят друг друга |
| 20 | **B: CEL-вид** | Тег **`!expr`**: tagged scalar, **операторы**/`$Name`; на **любом value-узле**. **`!ref` — доступ (82)**, не сахар над всей формулой. **Норматив грамматики — 79**. Host снят **75–76**. **`??` на целом `!expr` (83)**. Нет вычисления без оператора / dyn-index / `$` в list-map — **85** | Формулы без Go `tpl` и без дерева `!gt`. Не Starlark-документ |
| 21 | **P2** | В YAML bind/пути **только `$Name`**. `!expr`: лексер снимает `$` у `BindingName`, затем грамматика knarr (**79**). Строковые литералы не трогать. **`!not` без изменений**. **`!ref`:** trails + `['ключ']` (**48**). Среда имён без `$`. Regex по всей строке **запрещён** | Один словарь. `$` не ident |
| 22 | **`!not`** | Tagged scalar, только `RefPath`. Без `??` (**86**). Invert optional — bind `!ref … ??` затем `!not`, или `!expr` | Узкий not; не обёртка `!empty` |
| 23 | **нет host в `!expr`** | Вызовы `ident(` запрещены. `len`/`printf`/`has`/`fail` — теги или ошибка | Формула ≠ sprig |
| 24 | **`?.` + `??`** | **`?.` / `?[`** в декларации; перед оценкой внутренний **`get`**. **`??`:** ровно один, на **весь** `!ref` или **весь** `!expr` (**82**, **83**). **`?.` в путях `!expr` остаётся (92).** Автор не пишет `has()` / `get()`. Не `?:` / ` : ` / `!?:` | `??` без `:`; не тернарник. `?.` не токен грамматики |
| 25 | **ключ `?:`** + **32** + **51** + **52** + **53** + **60** + **108** | Suffix **`?`** в `!emit`/`$yield` и **`$Name?:`** на bind. **`$yield?:`** — пропуск итерации; пустой результат на **`имя?:`** — **108**. Лист: omit = `?:` **и** `?.` / `$Name?`. Нет `$when?:` / `$filter?:` / **`$over?:`**. Пара: `tls?: !ref $Tls?` | Нет тихого `spec: {}` |
| 26 | **strict / soft** | Нет `!policy` + есть `!$Type` → **strict**. Soft: нет ключа без default → omit (не `null`). Extra в soft можно | Схема не прячет дыры путей |
| 27 | **policy B** | **`!policy` только схема `!$Type`**. **`!policy` без typed bind — ошибка**. Нет `!policy` и нет `!$Type` — ок. `!ref` missing без `?` всегда ошибка | Один рычаг — схема. Soft не прячет дыры в путях |
| 28 | **D `!$Type`** | Тег типа — **`!$` + BindingName**. `!ValuesType` ошибка. Не синоним `!ref $T`. Default в поле схемы | Тег = имя типа |
| 29 | **bind B** | Документ bind = **`!bind`** (mapping, ≥1 `$Name`). Голый `$Name`-документ — ошибка. **`$Name: !bind` — ошибка**. Несколько ключей и несколько `!bind` можно | Все виды документов с тегом, как `!emit` |
| 30 | **Helm E1** | *(снято **31**)* | конвенция bind снята |
| 31 | **резерв v1** | **`$Release` `$Chart` `$Capabilities`** зарезервированы. Нельзя как bind. Нет инжекта/host. Helm-объекты **не в v1**. Comparison — **131** | Имена под будущее; в v1 не решаем |
| 32 | **collapse `?:`** | Все дети `?:` ↔ родитель `?:`. Пустой `{}` → omit. **`[]` — значение, не схлопывание (94).** Иначе ошибка | Presence-if на блок без `!when` |
| 33 | **`!match` value** | Предикат / else в поле: тег **`!match`** на **значении**. Omit — `ключ?:` без `$else-yield`. Не теги на ключах, не `!when`. Носитель веток — **37** | YAML не тегирует ключи; документный `$when` остаётся |
| 34 | **`$yield: !$T`** | `!$Type` на mapping **`$yield`**: каждый элемент `!foreach` как typed instance (схема, default, policy). Не тип списка `$Items`. Не общее вложенное `!$U` в bind/emit | Один тег на узел: `$Items: !foreach` нельзя совместить с `!$T`; элемент — `$yield` |
| 35 | **`!$Type` v1 A** | В v1 тег только на **корне `$Name`** и **`$yield`**. Нет `nested: !$U`, нет `!$Type` в `!emit` / `$yield`, нет типа на `$over` / списке `$Items` | Схема — данные графа, не K8s-выход |
| 36 | **`!match` A** | `!match` — обычное значение (`!expr`): `$yield`, bind, `$yield`/`$else-yield`. Вложенный `!match` прозрачен. Short-circuit. Сорт: конкретные узлы; `!ref`/`!expr` непрозрачны | Один конструктор; Helm if-in-range |
| 37 | **`!match` mapping** | Носитель — **mapping** `$if` / `$yield` / `$else-yield`. Не sequence веток, не несколько `$if` в одном теге. Else-if = `$else-yield: !match` | Список веток выглядит как «значение может быть разным» |
| 38 | **`!emit-foreach` A** | Документ **`!emit-foreach`**. **`!foreach`** на дочерних полях. **`$items` снят**. Нет splat только `$over` | Корень `!emit` — один манифест; range ресурсов — отдельный тег |
| 39 | **`$filter`** | Опц. bool на `!foreach` и `!emit-foreach`. Ложь → нет элемента / нет документа. Как `$if`. Невзятый `$yield` не считают | Helm range+if; не omit-дырка |
| 40 | **`$when` на `!emit-foreach`** | Опц. bool; ложь → пустой stdout, `$over`/`$filter`/`$yield` не считают. Нет `$yield`/`$else-yield`. `$as` в `$when` нельзя | Не `has()`; ворота пачки ≠ `$filter` |
| 41 | **`$over` map** | У **`!foreach` / `!emit-foreach`:** `$over` sequence или mapping (**113**). `$as` = значение; опц. `$key: $Name`. `$key` только с map. У **`!join`:** только sequence (**114**); элемент — coerce **`!str` (147)** | Helm range dict; порядок = источник |
| 42 | **нет `$index`** | У `!foreach` / `!emit-foreach` **`$index` нет** (не в v1). Map: опц. `$key` | Не плодить счётчик |
| 43 | **`$yield` B** | `!foreach` — любой сорт `$yield`; `!emit-foreach` — только mapping. Не flatten | List-of-string без обёртки `{ v: }` |
| 44 | **`!read` / `!import`** | `!read` — значение (данные YAML). `!import` — только документ, сплайс knarr. Не `labels: !import`. Пути — **13** | Два тега, два закона |
| 45 | **нет `!with`** | Нет тега `!with` / ключа `$with`. Helm `.` внутри блока = `ключ?: !ref $X.obj?` | Не скоуп Helm |
| 46 | **`!import` flatten** | Документы файла вставляются на место `!import`. После flatten — один поток; `!policy` только первый | Нарезка файлов |
| 47 | **нет `!define` / `!include`** | Фрагмент поля через файл — не в v1. Макросов нет | Не плодить include |
| 48 | **`['ключ']` в `!ref`** | Не-Ident ключ — `['…']` в `!ref` (кавычки YAML). Тега **`!path` нет**. В `!expr` — `labels["k"]` | Helm `index` |
| 49 | **нет `!walk`** | Тега **`!walk` нет навсегда**. Пути — `!ref` / `!expr` / `!foreach` | Не обход дерева отдельным тегом |
| 50 | **dyn-index `!expr`** | `$X[$K]` / `$X[$I]` только в **`!expr`**. В `!ref` индекс только `[n]`, не `$I`. Отрицательный нет | Динамика ≠ статический путь |
| 51 | **`$Name?:` C** | Optional bind: везде `$Name?` (+ trail). Ключ `?:`. Не `$Tls` без `?`. **Проверка ссылок — 120** | Пара маркеров; `$Tls?.cert` ок |
| 52 | **`$yield?:` B** | Omit `$yield` — пропуск итерации, не дырка. Пара как 25/51. `$yield?: !$T` нет. `$filter?:` нет. **`$over?:` нет (53)**. Пустой результат на **`имя?:` / `$Name?:` + `$yield?:`** — **108** | Helm range без `has()`; список сжимается |
| 53 | **нет `$over?:` A** | **`$over?:` ошибка (остаётся).** Omit **значения** `$over` у `!foreach` / `!emit-foreach` — **97** / bind **100**; у `!join` — **99**. Служебный ключ `?:` не плодить | Не плодить служебный `?:` |
| 54 | **теги `!` A** | Все теги knarr — **локальные `!…`**. **`!!ref` / `!!bind` / `!!$T`** — ошибка, не синоним. Не `%TAG` knarr | YAML local DSL |
| 55 | **нет `!!` в knarr A** | В документах knarr **любой `!!`** — ошибка (`!!str` / `!!int` / `!!null` в т.ч.). Скаляр — plain/quoted YAML 1.2. **`!read`**: core `!!str`/`!!int` да; **`!!null`** = YAML null (**130**) | Один handle в программе |
| 56 | **`--trace` формат** | След в stderr, stdout не меняет. Формат **не норматив v1** — [CLI_TODO_v2.md](CLI_TODO_v2.md) | Отладка без семантики |
| 57 | **exit** | **0** = успех (в т.ч. только `$warning`). **`$fail` → 1**. Прочие ошибки ≠ 0. Схема 2/3 — не v1 (**126**) | Один смысл fail |
| 58 | **щели Helm список** | «Прочие щели» — перечень в SPEC_TODO (потом CLI_TODO / v2). Разбор по одному. §1 не сужаем | Иначе щели не разбираются |
| 59 | **`!concat` A** | Склейка sequence — тег **`!concat`** только **bind**. В `!expr` нет `concat`/`+` на списках. Манифест: `!ref $Args`. **`$Name?:` — 104** | Декларативно; не формула в поле |
| 60 | **пустой foreach** | Пустой результат при живом `$over` + **`$yield:`** → **`[]`**. **`имя?:` / `$Name?:` + `$yield?:`** + печатать нечего → omit (**108**). Omit `$over` + **`имя?:` (97)** / **`$Name?:` (100)** — omit, цикл не считают. | `$yield:` оставляет `[]`; два `?:` — сахар |
| 61 | **`!validation` A+** | Документ **`!validation`**: `$rules` + XOR **`$fail`** / **`$warning`**. Нарушение: `$fail` → stderr, нет stdout, **exit 1**; `$warning` → stderr, emit как обычно, exit 0 | Helm required/fail декларативно; не `fail()` в `!expr` |
| 62 | **`!join`/`!split` A** | Только **bind**. `!join`: `$sep`+`$over` (seq строк) → string; **`$prefix`/`$suffix` — 115**. **`$Name?:` — 99**. `!split`: `$sep`+`$of` (string) → seq; **имя `$of` — 101**; **`$Name?:` — 102**. Нет host в `!expr` | Как `!concat`; не формула в поле |
| 63 | **`!sha256` A** | Только bind. `$of` string → hex lowercase UTF-8. **≡ Helm `sha256sum` той же строки.** **`$Name?:` — 103**. Нет host. Нет тихого toYaml | Checksum annotation; как 62 |
| 64 | **`!merge` A** | Только bind, tagged sequence mapping. Deep, later-wins, seq replace, map↔не-map ошибка. **`$Name?:` — 105**. Нет `mergeOverwrite`, нет host `merge` | Как `!concat`; Helm `merge`/`mustMerge` |
| 65 | **`!range` A1** | Только bind. XOR **`$to` in** / **`$until` out**. **`$from` / `$step` / `$Name?:` — 107**. Нет host `until`/`seq` | Очевидный `$to`; Helm `$until` |
| 66 | **`!empty` / `!and` / `!or`** | **`!empty` / `!not-empty`**: tagged scalar `RefPath`. Empty — **128**. **`!and` / `!or`**: sequence предикатов, считают всех. **`!not` не оборачивает** `!empty`/`!and`/`!or` | Helm empty / and / or декларативно |
| 67 | **`!int`/`!str`/`!bool` B** | Tagged scalar `RefScalar` на значении. В `!expr` coerce нет (сначала bind). Host/`!!int` нет. **`!float` — 81** | Helm int/toString; не parse-time YAML |
| 68 | **литералы list/map A** | В `!expr` и **справа `??` (82, 83)** list `[1, $X]` и map `{'k': $X}` / `{"k": $X}`. Ключи quoted. Не YAML `{a:1}`. Не concat. Вид как у CEL; норматив — knarr (**79**). Константа без `$` как весь `!expr` — **85** | `?? [80]`; уже были `[]`/`{}` |
| 69 | **`!read` один документ** | Ровно один YAML-документ. JSON как YAML 1.2 — ок. Multi-doc — ошибка (`!read-docs` — v2). Сырой текст нет | Один тег, одно дерево |
| 70 | **якоря** | В knarr-документе `&` / `*` / `<<` — ошибка. В **`!read`** якоря YAML 1.2 можно; в граф — дерево | Данные ≠ программа |
| 71 | **JSON-теги** | **`!to-json-str` / `!from-json-str` / `!sha256-json`**. Канон ≡ Helm `toJson` (Go Marshal). **`!to-yaml-str` — v2**. Поле JSON `null` ≡ нет ключа (**130**) | Checksum как Helm |
| 72 | **нет `!keys` / `!values`** | Список ключей/значений — `!foreach` + `$key` / `$as`. Host `keys`/`values` нет | Не плодить тег |
| 73 | **`!pick` B** | N-way omit: tagged sequence ≥2. Последний не omit. Первое не-omit. **Всегда значение: `$Name?: !pick` / `имя?: !pick` / `$Res?: !pick` — ошибка (105).** `??` бинарный **на весь скаляр (83)**. Не Helm `coalesce` (empty). **Тег остаётся (91)**. **≥2 не сужать (93)** | N-way без смены `??` |
| 74 | **нет `!last`** | Последний элемент — `$I` + `$Xs[$I]` в `!expr`. Нет `$Xs[-1]`, нет host `last` | Инвентарь закрыт |
| 75 | **format-тег D** | Tagged sequence. Глаголы **80**. Host `printf` нет. **`+` строк нет**. **`$Name?:` — 76**. Носитель — **149** (не только bind). Имя тега — **78** | Сборка строк — тег |
| 76 | **b64/len D + `?:`** | **`!b64enc`/`!b64dec`/`!len`**: tagged scalar как `!int`. Host нет. **`$Name?: !len $X.y?`** и **`$Name?: !format`** при omit-детях | линия тегов; пара 51 |
| 77 | **служебный ключ `$`** | На knarr-meta каждый служебный ключ с `$`. `when:` / `over:` / `sep:` / `of:` — ошибка, не синоним. Ключи манифеста без `$` | Не путать с K8s |
| 78 | **имя `!format`** | Тег **`!format`**. **`!printf` — ошибка**, не синоним | Один тег |
| 79 | **грамматика `!expr`** | Норматив — грамматика knarr (CEL-вид токенов, не CEL spec). Лексер снимает `$` у BindingName (**21**) | Не подключать CEL runtime |
| 80 | **диалект `!format`** | Go `fmt.Sprintf` на скалярах. Нет `%n` `%p` `%T` `%w`. Нет Rust `{}`. Mismatch — ошибка, не `%!s(int=…)` | Как Helm printf |
| 81 | **float B2** | Сорт **f64**. Inf/NaN ошибка. int+float в `!expr` → float. JSON ≡ Go. **`!float`**. `"500m"` string. Нет Quantity/decimal | Helm `0.5`; `%f`; 80 |
| 82 | **`!ref` / `!expr`** | **`!ref`** = доступ: путь, `?.`, бинарный **`??`** (весь скаляр). **`!expr`** = вычислить: операторы, пути, `$` в list-map, dyn-index (**50**). N-way — **`!pick`**. **`??` на целом `!expr` — 83**. Нет вычисления — **85**. **`??` на `!not` нет (86)**. **Оба тега остаются (90)**. **`?.` в формуле (92)** | Достать поле ≠ посчитать |
| 83 | **`??` целое `!expr`** | Ровно один top-level `??` на **весь** `!expr`. Спрос — **88**. Omit ≠ false. Чистый путь в `!expr` — **84**. Подывыражение — **87**. | Формула с omit без bind |
| 84 | **`!expr` не путь** | Слева `??` (или весь скаляр) — не чистый `RefPath`. Совпадение `!expr`/`!ref` на пути — **не v1** (v2). Что считается вычислением — **85**. **CLI-текст — 118.** | 83 не для поля |
| 85 | **`!expr` считает** | В теле (слева `??`) обязан быть **оператор**, или **dyn-index** `$M[$K]`, или **`$Name` в list/map-литерале**. Иначе ошибка: нет вычисления. `true` / `1` / `[80, 443]` / `{'k': 1}` — YAML (поле, `$when: true`, `$over: [80, 443]`) или `!ref`. `[80, $Port]` / `{'app': $X}` в `!expr` — ок. | 84 до конца |
| 86 | **нет `??` на `!not`** | `!not` — только `RefPath`, без `??`. `!not $X?.y ?? false` — ошибка. Invert optional: bind `!ref … ??` потом `!not`, или `!expr "!$X?.y ?? false"` (omit → **false**). **`??` на `!not` — v2.** | Путаница с `!($x) ??` (83) |
| 87 | **нет `??` в подывыражении** | `??` — разрез всего скаляра, не Pratt. `($a ?? 0) + ($b ?? 1)` — ошибка. Разные default — два `!ref` в bind, потом `!expr "$A + $B"`. Цепочка — **`!pick` (73)**. Синтаксис `??` так и в v2. | Подтверждение 83 |
| 88 | **спрос операндов** | `\|\|` / `&&`: только спрошенные (Kleene); `true \|\| omit` = **true**, `??` не берут. `+` `-` `*` `/` `%` / сравнения: **оба**. Missing без `?.` — ошибка 8. Так и в v2 (семантика оператора не менять). | Подтверждение 83 |
| 89 | **кавычки = YAML 1.2** | Tagged scalar (`!ref` / `!expr` / `!not` / coerce) — plain или quoted **как YAML 1.2**. `[` `{` `]` `}` `,` и `: ` / ` #` — кавычки **везде**, не «только с начала строки». `/` без кавычек ок. `?? false` / `\|\|` без кавычек ок. Вольность парсера (`?? [80, 443]` без кавычек) — не knarr. **Отвергать на слое knarr + golden — 119.** Так и в v2. | Носитель YAML, не лексер knarr до YAML |
| 90 | **оба тега** | **`!ref` и `!expr` остаются.** Не сливать операторы в `!ref`. Не снимать `!ref` в пользу пути в `!expr`. Синтаксис обоих тегов так и в v2. Совпадение на чистом пути — **84** (v2), не слияние. | Подтверждение 82 |
| 91 | **`!pick` остаётся** | N-way omit — **`!pick`**, не стопка `??` и не `\|\|`. `??` бинарный. `\|\|` — bool, не coalesce строк. Синтаксис `!pick` так и в v2. **≥2 (93)** | Подтверждение 73 |
| 92 | **`?.` в `!expr`** | Optional-шаг **`?.` / `?[`** в путях формулы — ок. Не требовать промежуточный `!ref`. Чистый путь без оператора по-прежнему ошибка (**84**). **Без `??` на обязательном ключе — пара (98), не запрет `?.`.** Так и в v2. | Подтверждение 24 / 82 |
| 93 | **`!pick` ≥2** | Два элемента — законный `!pick`, не только ≥3. Канон доков: 2-way — `??`; 3+ — `!pick`. Нижнюю границу в v2 не менять. **Нет warning (96)**. **Golden ≡ `??` — 116** | Подтверждение 73 / 91 |
| 94 | **`[]` не omit** | Пустой sequence — **значение**. Ключ `?:` + `!ref` пустого списка → `ключ: []`. Не схлопывать как `{}`. Omit foreach из пустого результата — только **`имя?:` + `$yield?:` (108)**. Иначе omit ключа — **omit `$over` + `имя?:` (97)**. Пропуск empty (Helm) — **`!not-empty` + `!match` на `?:`**, или **108**. Не `!empty` как значение поля. Так и в v2. | Подтверждение 32 / 66 |
| 95 | **`\|\|` не имена** | `\|\|` / `&&` — только bool / omit. Строка → ошибка типа, не `"myname"`. Имена: `!ref … ??` или **`!pick`**. Доки: wrong-пример `fullnameOverride \|\| name`. **Отдельного CLI-текста нет (117).** | Подтверждение 88 / 91 |
| 96 | **нет warning на 2-way `!pick`** | Два элемента и `??` — оба молча ок. Нет stderr/trace-предупреждения «короче `??`». Канон доков без смены закона. **Golden — 116.** Так и в v2. | Подтверждение 93 |
| 97 | **пара `?:` / `$over`** | `имя?: !foreach` ↔ `$over` omit-способный (`?.` / `$Name?`) **или** **`$yield?:` (108)**. Omit `$over` → omit ключа, цикл не считают. `имя?:` + `$yield:` + живой `$over` → `[]`. `имя?:` + обязательный `$over` + `$yield:` — ошибка пары. `имя?:` + `?? []`/`{}` + `$yield:` — ошибка пары. **`имя?:` + `?? []`/`{}` + `$yield?:` — ок (109).** `имя: !foreach` ↔ `$over` значение; пусто → `[]`. **`!emit-foreach`:** omit `$over` → **ноль документов**. **`$over?:` нет.** **`!join` — 99.** **Bind-foreach — 100.** Так и в v2. | Один закон с `!ref` / `??` |
| 98 | **пара `!expr` = `!ref`** | `replicas: !expr "$Values?.n + 1"` — ошибка пары (omit на ключе без `?:`). `replicas?:` или `?? 1` — ок. **Не** запрещать `?.` в формуле без `??`. Тот же закон, что `replicas: !ref $Values?.n`. Так и в v2. | Похожие конструкции одинаково |
| 99 | **пара `!join` = foreach** | `$Name?: !join` ↔ `$over` omit-способный (без `?? []`). Omit `$over` → omit bind. `$Name: !join` ↔ `$over` значение (`?? []` или обязательный путь); пусто → `""`. `$Name?:` + `?? []` / `$Name:` + omit `$over` — ошибка пары. **Пустой `$over` на `$Name?:` — `""` (110), не omit.** **`$over?:` нет.** **`!split` — 101 / 102.** Так и в v2. | Похожие конструкции одинаково |
| 100 | **пара bind-foreach** | `$Name?: !foreach` ↔ `$over` omit-способный **или** **`$yield?:` (108)**. Omit `$over` → omit bind. `$Name?:` + `$yield?:` + печатать нечего → omit bind. `$Name: !foreach` ↔ `$over` значение; пусто → `[]`. `$Name?:` + `?? []`/`{}` + `$yield?:` — ок (**109**). `$Name?:` + `?? []` + `$yield:` / `$Name:` + omit `$over` / `$Name?:` + обязательный `$over` + `$yield:` — ошибка пары. Тот же закон, что `имя?: !foreach`. Так и в v2. | Похожие конструкции одинаково |
| 101 | **`$of` у split** | У `!split` вход — **`$of`** (string). **`$over` на split — ошибка**, не синоним. `$over` = коллекция (`!foreach` / `!join`). `!sha256` тоже `$of`. Так и в v2. | Сорт входа ≠ join |
| 102 | **пара `!split` = join** | `$Name?: !split` ↔ `$of` omit-способный (без `?? ''`). Omit `$of` → omit bind. `$Name: !split` ↔ `$of` значение (`?? ''` или обязательный путь); пусто → `[]`. `$Name?:` + `?? ''` / `$Name:` + omit `$of` — ошибка пары. **`$of?:` нет.** **`!sha256` — 103.** Так и в v2. | Похожие конструкции одинаково |
| 103 | **пара `!sha256` = split** | `$Name?: !sha256` ↔ `$of` omit-способный (без `?? ''`). Omit `$of` → omit bind. `$Name: !sha256` ↔ `$of` значение (`?? ''` или обязательный путь); `""` → хэш пустой строки. `$Name?:` + `?? ''` / `$Name:` + omit `$of` — ошибка пары. **`$of?:` нет.** **`$over` на sha256 нет.** Так и в v2. | Похожие конструкции одинаково |
| 104 | **пара `!concat` = format** | `$Name?: !concat` ↔ есть omit-способный ребёнок (без `?? []`). Любой omit-ребёнок → omit **всего** bind. `$Name: !concat` ↔ все дети значения (`?? []` или обязательный путь); пусто / `[]` → `[]`. `$Name?:` + все дети с `?? []` / `$Name:` + omit-ребёнок — ошибка пары. Не пропуск сиблинга. **Не сужать (106).** **`!merge` — 105.** Так и в v2. | Похожие конструкции одинаково |
| 105 | **пара `!merge` = дерево `?:`** | `$Name?: !merge` ↔ **все** дети omit-способны (без `?? {}`). Результат = merge **живых**; все omit → omit bind. Литерал / обязательный ребёнок на `$Name?:` — ошибка пары (значение всегда есть). `$Name: !merge` ↔ все дети значения (`?? {}` или обязательный путь); пусто → `{}`. `$Name:` + omit-ребёнок — ошибка пары. **`$Name?: !pick` невозможно** — последний ребёнок всегда значение. Так и в v2. | Дерево `?:`; `!pick` всегда значение |
| 106 | **два закона sequence** | **`!concat` / `!format` не сужать** до «все дети omit». Один omit-ребёнок убивает весь bind; литерал рядом на `$Name?:` ок (fmt-строка / база). **`!merge`** — все дети omit-способны (105). Два закона по роли ребёнка. Так и в v2. | Format-строка ≠ overlay |
| 107 | **`!range` E+E2** | **`$from`** всегда значение на `$Name:` (нет ключа / omit — ошибка, не `0`). Ключа **`$step` нет → `1`**; ключ есть → только значение (`?? 1` / обязательный путь); omit написанного `$step` — ошибка. XOR `$to`/`$until`. **`$Name?: !range`** ↔ хотя бы один из `$from`/`$to`/`$until` omit-способен (без `??`); `$step` в пару не входит. Omit любого из трёх → omit bind. Все трое значения на `$Name?:` — ошибка пары. **`$from?:` / `$step?:` / `$to?:` / `$until?:` нет.** Так и в v2. | `$from` явный; `$step=1` — единственный default; `?:` как concat |
| 108 | **пустой foreach + `$yield?:`** | **`имя?:` / `$Name?:` + `$yield?:`** + печатать нечего (`$over` = `[]`/`{}` / все `$filter` ложны / все `$yield?:` omit) → **нет ключа / нет bind**. `имя?:` + `$yield:` + живой `$over` → `[]`. `имя:` + любой yield → `[]`. `$over: !ref $Values.env` + `$yield?:` → `имя?:` законно. **`?? []`/`{}` + `$yield?:` — 109.** **`$yield?` без `:` — ошибка.** **`!emit-foreach`** не меняется (пусто → ноль документов). Так и в v2. | Два `?:`: элемент и ключ могут пропасть |
| 109 | **`имя?:` + `?? []` + `$yield?:`** | **`имя?:` / `$Name?: !foreach` + `$yield?:` + `$over` с `?? []` / `?? {}`** — законно: missing → `[]`/`{}` → печатать нечего → нет ключа / bind. **`имя?:` + `?? []`/`{}` + `$yield:`** — ошибка пары. **`!join` / `!split` / `!sha256` / `!concat` — не менять** (`?:` + `??` на входе по-прежнему ошибка). Так и в v2. | `$yield?:` оправдывает `?:` после `??` |
| 110 | **пустой join — строка** | **`$Name?: !join` + живой `$over: []` → `""`**, не omit bind. Нет bind — только omit `$over`. Нет второго маркера как `$yield?:` — пустую склейку не схлопывать. **`!split`** пустой `$of` → `[]`; **`!sha256`** от `""` → хэш: тоже значение. Так и в v2. | `""` / `[]` / хэш — значения; foreach omit — только с `$yield?:` |
| 111 | **`имя?:` + `??` лист** | **`имя?: … ?? …` / `$Name?: … ?? …`** на листе `!ref` / `!expr` / coerce — **ошибка пары**. `??` → значение, ключ всегда есть. Default — ключ без `?:`. Omit — `?:` без `??`. Исключение только **`имя?: !foreach` + `$yield?:` + `?? []`/`{}` на `$over` (109)**. Так и в v2. | `?:` и `??` на листе не вместе |
| 112 | **bool-слот + omit** | **`$when` / `$filter` / `$if`** — только bool. Omit из `?.` без `??` — **ошибка**, не `false`. Писать **`?? false`** или **`!empty` / `!not-empty`**. Нет сахара omit→false. **`$when?:` / `$filter?:` / `$if?:` нет.** Так и в v2. | Omit ≠ false; нет скрытого default |
| 113 | **один `$over`** | У **`!foreach` / `!emit-foreach`** один ключ **`$over`**: sequence или mapping. Нет `$map` / `$seq`. Автор пишет **`?? []`** или **`?? {}`**. Неверный сорт — ошибка слота `$over`. Так и в v2. | Не плодить ключ; литерал явный |
| 114 | **join только seq** | **`!join` `$over`** — только sequence (не mapping / не **`?? {}`**). Элемент — **string** до **147**; теперь coerce **`!str`**. Ключи/значения map — **`!foreach`**, потом join. Так и в v2. | Слот join ≠ обход foreach |
| 115 | **`$prefix` / `$suffix` у join** | Опц. ключи **`!join`**. Нет ключа — нет обёртки. Ключ есть → как **`$sep`**: string, **непустой**; omit / `""` / не string — ошибка. **`$prefix?:` / `$suffix?:` нет.** Не входят в пару `$Name?:` (как `$sep`). Результат = prefix + join(`$over`, `$sep`) + suffix (пустой `$over` → prefix+suffix, напр. `"[]"`). На **`!split` / `!format` / `!concat` нет.** Так и в v2. | Обёртка после склейки; слот как `$sep` |
| 116 | **golden 2-way `!pick` ≡ `??`** | Плановый golden: два кандидата `!pick` и бинарный `??` на тех же операндах — **один stdout** (byte-for-byte). Негатива «2-way = ошибка» нет. Доки: канон 2-way — `??`. Так и в v2. | Доказательство 93 / 96 |
| 117 | **`\|\|` — общая ошибка типа** | String / int / seq / map у `\|\|` / `&&` — **общая ошибка типа** (ожидали bool). Отдельного stderr «пиши `!pick` / `??`» нет. Канон имён — доки. Так и в v2. | Не плодить канон CLI |
| 118 | **тексты `!ref` / `!expr`** | **`!expr` + чистый путь / константа без вычисления** → отдельный stderr: нужен **`!ref`** (или YAML-литерал). **`!ref` + оператор / dyn-index / `$` в list-map** → отдельный stderr: нужен **`!expr`**. Не общая «ошибка разбора» без подсказки тега. Так и в v2. | Два тега — две подсказки |
| 119 | **plain `?? [` отвергать** | `!ref $X?.p ?? [80, 443]` без кавычек — **ошибка knarr**, даже если YAML-парсер отдал одно тело. Разрез YAML на два узла — ошибка YAML, не второй закон. Плановый **негативный golden**. Так и в v2. | Подтверждение 89 |
| 120 | **`$Name?` в ссылках** | Объявление **`$Name?:`** ↔ во всех **`!ref` / `!expr` / `!not` / coerce** корень только **`$Name?`**. Голый `$Name` / `$Name.cert` — ошибка. Обязательный bind + `$Name?` — ошибка. Проверять, не синоним. Так и в v2. | Подтверждение 51 |
| 121 | **omit.md — хаб** | Снято **144**: omit.md удалён; раздел Omit на странице конструкции. | хаб → разнос |
| 122 | **Helm Comparison** | В guide секция Helm: пара либо **тот же результат** (расхождение — в Difference: quote, порядок), либо knarr **невозможно** — без притворного примера. Больше точных пар и явных невозможностей. Нет «почти как Helm». Аудит таблиц — отдельный проход. Так и в v2. | Честный migrate |
| 123 | **README Purpose** | В [docs/README.md](../docs/README.md) Language guide — колонка **Purpose**: одно предложение. Страница тега — норма; таблица не копирует примеры. Так и в v2. | Оглавление «зачем» |
| 124 | **`---` в примерах docs** | Два корневых тега документа (`!bind` / `!emit` / `!emit?` / `!emit-foreach` / `!emit-foreach?` / `!emit-range` / `!emit-range?` / `!validation` / `!import` / `!policy` / `!typedef`) в одном YAML-примере — **`---` между ними**. Один документ / поле — без. Так и в v2. | Пример = законный носитель |
| 125 | **CLI_TODO** | Вопросы CLI v1 — только [CLI_TODO.md](CLI_TODO.md). [SPEC_TODO.md](SPEC_TODO.md) — только язык. Scaffold CLI / evaluator / goldens / chart — в CLI_TODO; реализация — по явному запросу. Так и в v2. | Не смешивать язык и продукт |
| 126 | **CLI_TODO_v2** | CLI «не в v1» — только [CLI_TODO_v2.md](CLI_TODO_v2.md): формат `--trace`, схема exit 1/2/3, несколько positional, stdin / `-`. Не держать их в [SPEC_TODO_v2.md](SPEC_TODO_v2.md). Так и дальше. | Та же схема, что у языка |
| 127 | **`?` на поле пути** | Optional шаг пути — **суффикс `?` на этом поле**, в том числе последнем: `$Values.deployWorkers?`, `$Values.ingress?.enabled?` (`ingress?` и `enabled?`). Оператора **`?.` нет**. `$Name?.cert` = optional bind **`$Name?`** + обязательный `.cert`. `$Values?.tls` = `$Values?` + `.tls` (нужен `$Values?:`). `??` только после omit-шага. `?[` → `[n]?` / `['k']?`. Так и в v2. | Тот же `?`, что `$Name?` / `имя?:` |
| 128 | **empty + float 0** | **`!empty`** true: omit, `""`, `[]`, `{}`, `false`, int **`0`**, float **`0.0` / `-0.0`**. Иначе false. **`!not-empty`** — отрицание. Как Helm `if` / `empty` на нуле. `.nan` / `.inf` по-прежнему ошибка числа. Так и в v2. | Helm `if 0.0` ложен |
| 129 | **Helm `required` = `!validation`** | В guide: Helm **`required`** при нарушении **прерывает**, stdout нет, только stderr. Это **`!validation`** + `$fail` + `!not-empty` на том же пути (`?`). Успех `required` — значение в шаблоне; knarr печатает его **`!emit` / `!ref`**, не `!validation`. Рядом с каждым `required` в паре — документ `!validation`. Текст `$fail` / required — только Difference. Так и в v2. | Один смысл fail |
| 130 | **`a: null` ≡ нет поля** | YAML/JSON **`null` / `~` / `a:`** на ключе mapping ≡ **ключа нет** (omit), не значение. То же во входе (`!read`, литерал, `!from-json-str` поле объекта). Корень документа / JSON `null` ≡ omit всего значения. Элемент sequence **`null`** — ошибка (omit в seq). Stdout **`null` не печатает**. В Comparison Helm **`a: null`** ≡ knarr без `a`. `$else-yield: null` ≡ нет `$else-yield`. `!!` в knarr-документе по-прежнему ошибка; в **`!read`** `!!null` = тот же null. Так и в v2. | Один смысл «нет»; Helm `toYaml` nil |
| 131 | **Comparison `.Release` — Impossible** | В guide Helm **`.Release.Name`** (и `.Chart` / `.Capabilities`) — **Impossible**. Не показывать пару с авторским `$Rel` / `$Instance`. Резерв имён и нет инжекта — как уже принято: `$Release` нельзя bind, host-объектов нет. Так и в v2. | Не притворяться Helm Release |
| 132 | **`$yield` — весь документ** | Helm `if` с несколькими ключами (`kind` + `name`) = один `!emit`: `$when: !not-empty` на пути с `?`, `$yield` — весь mapping, `$else-yield: ""`. Не Impossible и не второй документ на лишний ключ. Helm `if` ≡ `!not-empty`. Так и в v2. | `$yield` = тело emit |
| 133 | **`!emit?`** | Документ **`!emit?`**: только **`$yield?:`**, **без** `$else-yield`. **`$when` опционален (155).** Ложь `$when` → нет документа (`$yield?:` не считают). Ключ `$when` есть, значение omit — **ошибка (112)**. `!emit?` + `$else-yield` / без `$yield?:` / **`$yield:`** — ошибка пары. Omit `$yield?:` → нет документа (**153**). **`!emit` + `$when`** требует `$yield` и `$else-yield`. **`!emit-foreach?` / `!emit-range?` — 148.** `$Name: !emit?` — ошибка. Так и в v2. | `?` на документе ↔ `$yield?:` |
| 134 | **Helm `default` = `!match` + `!empty`** | В guide: Helm **`\| default`** (empty → запасное) = **`!match`** `$if: !empty путь?` / `$yield` default / `$else-yield` живое значение. Не `??` и не `!pick` (те — только omit). Цепочка `default` — вложенный `!match`. Отдельного тега empty-default нет. Так и в v2. | Helm empty, не omit |
| 135 | **`if .Values.tls` поле — Impossible** | Снято **140**: `tls?: !skip-empty`. Исторически: не пара с `tls?: !ref`. | не «почти как Helm» |
| 136 | **Helm `and` + `gt` — пара** | В guide Helm **`if and .Values.service.enabled (gt .Values.replicas 1)`** = **`!emit?`**: `$when: !and` из **`!not-empty $Values.service?.enabled?`** и **`!expr "$Values.replicas? > 1 ?? false"`**, `$yield?:` — документ. Missing → omit → `!not-empty` ложь / `>` omit → `?? false`. Не Impossible. Сорт `replicas` (строка vs int) — только Difference. Так и в v2. | Helm `if` ≡ `!not-empty`; `gt` ≡ `>` |
| 137 | **Helm `if` + `range` документов — пара** | В guide Helm **`if .Values.deployWorkers` + `range .Values.workers`** нескольких манифестов = **`!emit-foreach`**: `$when: !not-empty $Values.deployWorkers?`, `$over: !ref $Values.workers?`. В **Helm сниппете Comparison — `---`** перед каждым ресурсом (как на странице `!emit-foreach`). Без `---` это другой stdout — не пара. Не Impossible. Так и в v2. | `if` ≡ `!not-empty`; range документов ≡ `!emit-foreach` |
| 138 | **Helm `range` + `if and .ports .enabled` — пара** | В guide Helm **`env:` + `range` workers + `if and .ports .enabled` + `- name:`** = **`env?: !foreach`**: `$over: !ref $Values.workers?`, `$filter: !and` из **`!not-empty $Worker.ports?`** и **`!not-empty $Worker.enabled?`**, **`$yield?:`** mapping `{ name: }`. Не Impossible. Не пара с `!and` сырых полей, не скалярный `$yield`, не `env:` без `?:`. Так и в v2. | `if` ≡ `!not-empty`; элемент списка ≡ mapping `$yield` |
| 139 | **Helm `and` строк в поле — пара** | В guide Helm **`name: {{ and .Values.name .Values.image }}`** = **`name?: !match`**: `$if: !not-empty $Values.name?` / `$yield: !match` `$if: !not-empty $Values.image?` / `$yield: !ref $Values.image` / `$else-yield: !match` `$if: !not-empty $Values.name?` / `$yield: !ref $Values.name`. Go `and x y` = если x truthy то y иначе x. Не Impossible. Не `!and` строк. Так и в v2. | `and` как значение ≡ `!not-empty` + `!match` |
| 140 | **`!skip-empty` + `!is-empty` / `!is-not-empty`** | Тег **`!skip-empty`**: tagged scalar `RefPath`; empty как у `!is-empty` → **omit значения**, иначе то же значение. Только целое значение ключа, где omit уже законен: **`имя?:`**, **`$Name?:`**, **`$yield?:`**. Иначе ошибка (`имя:`, `$when`, `$yield`, `$over`, seq, ребёнок `!pick`). Нет `!skip-not-empty` / `!omit-empty`. Предикаты переименованы: **`!is-empty`** / **`!is-not-empty`**; **`!empty` / `!not-empty` — ошибка**, не синоним. Comparison **`if .Values.tls` + поле** = `tls?: !skip-empty` (снимает Impossible **135** для этого сниппета). `$yield: !skip-empty` у **`!emit`** запрещён. **`$yield?: !skip-empty`** у **`!emit?`** — слот omit (**153**). Так и в v2. | `is-` = bool; `skip-empty` = поле; только ключ `?:` |
| 141 | **`!skip-empty` в `$yield`/`$else-yield` `!match`** | **`!skip-empty`** ещё как целое **`$yield` / `$else-yield` у `!match`**, если сам `!match` в omit-слоте: `имя?:` / `$Name?:` / `$yield?:` или вложенный `$yield`/`$else-yield` такого `!match`. Не `$yield` у `!emit`. У **`!emit?`** — **`$yield?:`**. Не ребёнок **`!pick`**. Не **`$over`**. На `имя: !match` + `$yield: !skip-empty` ловит пара omit без `?:`. Канон Helm `and` двух строк: `name?: !match` `$if: !is-not-empty $Values.name?` / `$yield: !skip-empty $Values.image?`. Так и в v2. | слот результата match, не орфография `$yield?:` |
| 142 | **Helm `or` / leftover `if` — пары** | В guide: **`host: {{ or .Values.host "localhost" }}`** = **`!match`** `$if: !is-empty $Values.host?` / `$yield: localhost` / `$else-yield: !ref $Values.host` (как `\| default`, не `??`, не `!skip-empty` на ключе). **`if or .Values.ingress.enabled .Values.mesh.enabled`** = **`!emit?`** + `$when: !or` двух **`!is-not-empty`** (как `if and` + `gt`). **`if and .Values.service.enabled .Values.tls`** документ `kind: Service` = **`!and`** двух **`!is-not-empty`** (не Impossible; не поле `tls?:`). **`if .Values.ingress.tls`** документ = **`!is-not-empty`**. **`if not .Values.service.enabled`** = **`!is-empty`**. Range **`if not .disabled`** = `$filter: !is-empty`. **`if eq .Values.ha "true"`** только `kind` = **`!emit?`** `$when: !expr "$Values.ha == 'true'"` / `$yield?: { kind: … }`. Не `!or` строк. Так и в v2. | empty-default и `if` уже были; Comparison догнать |
| 143 | **Helm `range until` документов — пара** | В guide: **`range until .Values.completions`** + `kind: Job` / `name: {{ . }}` = документы из int-ряда (канон **144**: **`!emit-range`**). В **Helm сниппете Comparison — `---`**. Без `---` — не пара. `name:` Helm int / knarr `!str` — quote в Difference. Не `!range` в `$over`. **`printf "w-%04d"` в `$yield` — пара (`!format`, 149).** Так и в v2. | `until` документов ≡ ряд; `---` как у range документов |
| 144 | **`!emit-range` + `$yield`; omit.md снят** | **`!emit-range`:** документ, границы как **`!range`**, тело как **`!emit-foreach`** (`$as` / опц. `$when` / `$filter`; `$yield` mapping; нет `$over` / `$key`). **`$yield?:` на документном цикле — 148.** **`!range`:** bind; те же `$as`/`$when`/`$filter`/`$yield`; `$yield` любой сорт как у `!foreach`. **Нет** `$Idx: !range` `$from`/`$until` без `$yield` (ints только `$yield: !ref $I`). `$when` ложь ≡ пустой ряд (границы не считают): bind как пустой `!foreach`; emit → ноль документов. **omit.md удалён:** раздел **Omit** на странице конструкции (повторы ок). **121 снят.** Docs: вход `# $Var = {…}`, результат комментарием. Цикл==цикл — один вид слотов (щели — SPEC_TODO). Так и в v2. | ряд = цикл; хаб omit разобран |
| 145 | **`$when` у `!foreach`** | Опц. **`$when`** на **`!foreach`** как у **`!emit-foreach` / `!emit-range` / `!range`**: bool; omit — ошибка (**112**); **`$when?:` нет**; `$as` / `$key` не видны. Ложь → `$over` / `$filter` / `$yield` не считают; результат как пустой `$over` (`$yield:` → `[]`; `имя?:` / `$Name?:` + `$yield?:` + печатать нечего → omit ключа / bind). Не заменяет `$filter`. Так и в v2. | цикл==цикл |
| 146 | **`!range` как поле** | **`!range`** — значение как **`!foreach`**: поле `!emit` / `$yield` / bind. **`имя?: !range`** — та же пара, что `имя?: !foreach` (omit границы или `$yield?:`). Документ `!range` / `$over: !range` — ошибка. **`!emit-range`** — документы. Docs: каждый пример — `# $Var = {…}` и результат (`# name: …` или блок внизу). Так и в v2. | цикл==цикл; носитель как foreach |
| 147 | **`!join` не цикл; `$filter`; `!str`** | **`!join` не цикл:** нет `$yield` / `$when` / `$key`. **`$over`** — sequence (не map / не `?? {}`, **114**). Каждый элемент **перед склейкой** — как **`!str`**. Опц. **`$filter`** + **`$as`** (пара). `$filter` видит исходный элемент. Так и в v2. | склейка ≠ цикл |
| 148 | **`!emit-foreach?` / `!emit-range?`** | Пара как **`$yield:` / `$yield?:`** у `!foreach`. **`!emit-foreach` / `!emit-range`:** только **`$yield:`** (mapping); omit тела — ошибка. **`!emit-foreach?` / `!emit-range?`:** только **`$yield?:`**; omit значения → **нет документа** (не дырка). `$yield?:` на обычном теге / `$yield:` на `?`-теге — ошибка пары. `$when` опц. на обоих (**тот же закон, что `!emit?`, 155**). `$Name: !emit-foreach?` / `$Name: !emit-range?` — ошибка. Так и в v2. | документный `?:` на теге; `$yield?:` как у foreach |
| 149 | **`!format` как значение** | **`!format`** — значение, как **`!str`**: поле `!emit` / `$yield` / `$else-yield` `!match` / `$yield` / `$yield?:` / bind. Документ `!format` / вложенный `!format` — ошибка. **`имя?: !format`** / **`$Name?:`** ↔ omit-ребёнок. **`имя: !format`** / **`$yield:`** + omit-ребёнок — ошибка пары. Helm **`printf` в `$yield` ряда** — пара. **`!concat` / `!join` / `!merge` — 150.** Так и в v2. | `printf` ≡ поле |
| 150 | **`!concat` / `!join` / `!merge` как значение** | Как **`!format`:** поле `!emit` / `$yield` / `$else-yield` `!match` / `$yield` / `$yield?:` / bind. Документ / вложенный тот же тег — ошибка. Целое `$yield` у `!emit` — YAML-mapping, не тег склейки. **`$yield`** у `!emit-foreach` / `!emit-range` — только mapping: **`!merge`** ок; **`!concat` / `!join`** — ошибка сорта. Пары omit как в bind: **`!concat`** — один omit-ребёнок; **`!join`** — omit `$over`; **`!merge`** — все дети omit-способны. **`!split` / `!sha256` — 151.** Так и в v2. | склейка коллекций = поле |
| 151 | **`!split` / `!sha256` как значение** | Как **`!join` / `!format`:** поле `!emit` / `$yield` / `$else-yield` `!match` / `$yield` / `$yield?:` / bind. Документ — ошибка. Целое `$yield` у `!emit` — YAML-mapping. **`$yield`** у `!emit-foreach` / `!emit-range` — mapping: **`!split` / `!sha256`** — ошибка сорта (`!sha256` — string; `!split` — sequence). Пары omit как в bind: **`?:`** ↔ omit `$of`. **`!sha256-json`** уже был value-скаляр. Так и в v2. | не осталось bind-only склеек |
| 152 | **`$yield` / `$else-yield` у ветки** | Где смысл — выдать документ или значение: **`!match`**, **`!emit?`**, **`!emit` + `$when`**. Тело — **`$yield`** или **`$yield?:` (153, 154)**; иначе — **`$else-yield`**. **`$then` / `$else` — ошибка**, не синоним. **`$else-yield: ""`** на `!emit` = пропуск документа. На **`!emit` + `$when`:** только **`$yield`** + **`$else-yield`**. **`$else-yield?:` нет**. Цикл: `$yield` / `$yield?:` как раньше. Так и в v2. | ветка и цикл — одно имя тела |
| 153 | **`!emit?` ↔ `$yield?:`** | Как **`!emit-foreach?` / `!emit-range?`:** **`!emit?`** только **`$yield?:`** (mapping или omit); omit → **нет документа**. **`$yield:`** на `!emit?` / **`$yield?:`** на `!emit` — ошибка пары. **`$when` — 155.** **`$else-yield` нет.** **`!skip-empty`** на `$yield?:` законен. **`!match` — 154.** Так и в v2. | `?` на теге ↔ `$yield?:` |
| 154 | **`$yield?:` на `!match`** | Ровно один из **`$yield:` / `$yield?:`**. **`$yield:`** + опц. **`$else-yield`**. **`$yield?:`** — без `$else-yield` (ошибка пары). `$if` истинно + omit `$yield?:` → **omit результата**. `$if` ложь + `$yield?:` → omit (нет else). Только omit-слот: **`имя?:` / `$Name?:` / `$yield?:`** / вложенный результат такого `!match`. **`имя: !match` / `$Name: !match` + `$yield?:` — ошибка пары**. `$if?:` / `$else-yield?:` — ошибка. **`!skip-empty`** на `$yield?:` законен. Тега **`!match?` нет**. Так и в v2. | тело ветки как у `!emit?` |
| 155 | **`?` на документе — один закон** | **`!emit?` / `!emit-foreach?` / `!emit-range?`:** только **`$yield?:`**; omit → нет документа. **`$when` опционален** на всех трёх. Нет `$when` — считают `$yield?:`. Ложь `$when` → нет документа / ноль документов, тело не считают. Ключ `$when` с omit — ошибка (**112**). Нет `$yield?:` — ошибка пары. Тегов **`!foreach?` / `!range?` / `!match?` нет** (щель). Так и в v2. | один суффикс `?` |

## Naming
- Выбрано **knarr** (после отбраковки морских/YA*/латинских/других shortlist’ов).
- Не использовать: yasp (YA*), vosk (ASR), sborka (бренды), mutum (mute/птица), briv (SaaS).
