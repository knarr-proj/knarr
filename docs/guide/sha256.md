# `!sha256`

SHA-256 of a **string**, hex lowercase, UTF-8. Bind-only. Byte-identical to Helm `sha256sum` of the same string.

For a **tree**, use [`!sha256-json`](sha256-json.md).

**Helm:** `sha256sum` — [vs Helm](sha256-vs-helm.md).

## Syntax

```yaml
$Sum: !sha256
  $of: !ref $Values.password
```

Exactly `$of` (string). Mapping/seq is an error.

## Examples

### Annotation from a password string

```yaml
---
!bind
$PwHash: !sha256
  $of: !ref $Values.password
---
!emit
metadata:
  annotations:
    checksum/secret: !ref $PwHash
```

### Hash a formatted blob

```yaml
$Blob: !format
  - "%s:%s"
  - !ref $Values.user
  - !ref $Values.password
$Sum: !sha256
  $of: !ref $Blob
```

### Hash JSON (prefer the dedicated tag)

```yaml
$Sum: !sha256
  $of: !to-json-str $Values.config
```

Same as `!sha256-json $Values.config`.

## Common mistakes

**Wrong — `$of` a mapping**

Silent toYaml is forbidden. Use `!sha256-json`.

**Wrong — in `!emit`**

**Wrong — host `sha256sum()` in `!expr`**

## See also

- [vs Helm](sha256-vs-helm.md)
- [`!sha256-json`](sha256-json.md)
- [`!to-json-str`](to-json-str.md)
